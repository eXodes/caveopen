---
description: Post-model-upgrade §V audit — classify invariants as COMPUTATIONAL / INFERENTIAL / ADVISORY / DEAD
argument-hint: "[--trim]"
---

Invoke the `ck-audit` skill with the following arguments:

$ARGUMENTS
